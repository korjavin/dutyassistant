1. Modify `assignChore` to accept a pre-selected user or user ID (maybe string or `*store.User`). But wait, `assignChore` currently finds the best user randomly from all users. If we change it to accept a specific user, we can bypass the random generation. Let's make `assignChore` take an optional `*store.User` instead of randomly picking if `specificUser` is not nil.
2. In `HandleChore` (one-off chore path) and in `HandleChoreInteractive`, we should parse for the specific user before passing to `assignChore`. Wait, the issue says `/<name>` command. So we should modify the command parsing logic.

Ah, if the user types `/chore Fix the door knob /Crusader`
We can split the args to find the `/name`.

Let's rethink `assignChore` arguments.
We can add `specificUserName string` to `assignChore`, but maybe it's cleaner to just resolve the `specificUser` inside `assignChore` or pass `specificUserName` and let `assignChore` handle it?
If we do it inside `HandleChore`, `assignChore` is called in `HandleChore`, `HandleChoreInteractive` and `HandleChoreCron`.
Wait, in `assignChore`:
```go
func (h *Handlers) assignChore(chatID int64, fromUserID int64, description string, specificUserName string) (tgbotapi.MessageConfig, error)
```
If `specificUserName` is not empty, we find that user. If not found, we return an error message to the admin.

Let's write a small plan and request a plan review.
